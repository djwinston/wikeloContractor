namespace WikeloContractor.ViewModels;

public partial class MainWindowViewModel : ObservableObject
{
    // The title and navigation items are defined in MainWindow.xaml via DynamicResource
    // so that language switching works without a restart.
}
